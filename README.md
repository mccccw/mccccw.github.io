# MCCW GitHub Page

Static Vue-powered GitHub Pages site for the MCCW Minecraft server.

The page is built to run directly from GitHub Pages without a build step:

- `index.html` hosts the Vue template and SEO metadata.
- `app.js` mounts Vue 3 from the browser ESM build.
- `styles.css` contains the Project-Crowbar-inspired visual refresh.
